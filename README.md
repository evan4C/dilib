# dilib

all-in-one personal digital library for Mac OS

## Project plan

- Establish a `mediaItem` SwiftData model capturing media type (enum), title, author, tags, notes, file URL, date added, date modified, etc.
- Build a SwiftUI navigation layout
- Implement manual item management
- Provide polished visuals
- Create yearly report generator
